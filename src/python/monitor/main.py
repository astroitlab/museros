# coding=utf-8import sysimport window as acv# import window as vis# import window as png# import window as conimport screen as scrfrom PyQt4 import QtGuifrom env import *if __name__ == '__main__':    app = QtGui.QApplication(sys.argv)    if len(sys.argv)<2:        env = ENV(1)    else:        env = ENV(int(sys.argv[1]))    screen = scr.screen()    scrs = screen.splitscreen()    acvWindow = acv.MainWindow('vis',scrs[0])    #visWindow = vis.MainWindow('vis',scrs[1])    #pngWindow = png.MainWindow('png',scrs[2])    #comWindow = con.MainWindow('con',scrs[3])    sys.exit(app.exec_())